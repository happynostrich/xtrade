"""Demo EMA-cross strategy (placeholder).

Phase 1 Task 5 will lift the EMA-cross used in C6b
(`scripts/phase0/08_sample_backtest.py`) into a config-driven
`DemoEmaCross(XtradeStrategy)` that runs unchanged in both backtest
(via `xtrade backtest run`) and live testnet (via `xtrade live run`).
"""

from __future__ import annotations
