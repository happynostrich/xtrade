"""xtrade.node — TradingNode construction and health checks.

Phase 1 Task 3 will populate `factory.build_testnet_node(...)` and
`health.probe(...)`. This module currently exposes only the public
surface so other subpackages and the CLI can import-against it without
hitting NotImplementedError at import time.
"""

__all__: list[str] = []
