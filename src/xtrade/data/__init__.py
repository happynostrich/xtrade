"""xtrade.data — historical data ingestion and ParquetDataCatalog access.

Phase 1 Task 4 will populate:
  - binance_klines.fetch(...)
  - hyperliquid_hip3.fetch(...)
  - catalog.write_bars / read_bars / paths

Until then these submodules are empty placeholders that raise on call.
"""

__all__: list[str] = []
