"""Hyperliquid HIP-3 candle snapshot fetcher (placeholder).

Phase 1 Task 4 will implement candle fetching via Hyperliquid's
`info` endpoint (`{"type": "candleSnapshot", ...}`), targeting trade.xyz
symbols of the form `xyz:TSLA` etc. Read-only against mainnet.
"""

from __future__ import annotations


def fetch():  # pragma: no cover - placeholder for Task 4
    raise NotImplementedError(
        "hyperliquid_hip3.fetch is implemented in Phase 1 Task 4"
    )
